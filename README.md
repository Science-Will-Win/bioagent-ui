# 🧬 AIGEN BioAgent UI

AIGEN Sciences × Modulabs | Biomedical AI Agent Interface

## 아키텍처

```
┌─────────────────────────────────────────────────────┐
│ Presentation Layer (React 18 + TypeScript)           │
│ ┌──────┬──────────────┬───────────────────────────┐ │
│ │      │  LeftPanel    │  RightPanel               │ │
│ │ Tool │  ┌──────────┐ │  ┌──────────────────────┐ │ │
│ │ bar  │  │ QueryBox │ │  │ StepCard ×N          │ │ │
│ │      │  ├──────────┤ │  │  Thought → Action    │ │ │
│ │      │  │ ChatBox  │ │  │  → Result → Graph    │ │ │
│ │      │  └──────────┘ │  └──────────────────────┘ │ │
│ └──────┴──────────────┴───────────────────────────┘ │
│ State: Zustand (analysis | ui | stats | settings)    │
│ Realtime: WebSocket                                  │
└─────────────────────────────────────────────────────┘
         ↕ REST API + WebSocket
┌─────────────────────────────────────────────────────┐
│ Business Layer (FastAPI)                             │
│ Task Queue → Agent Executor → Logging Service        │
└─────────────────────────────────────────────────────┘
         ↕
┌─────────────────────────────────────────────────────┐
│ Agent Layer (Biomni A1)                              │
│ LLM (GPT-4o / Claude) → Tool Executor → Data Lake   │
└─────────────────────────────────────────────────────┘
```

## 기술 스택

| 영역 | 기술 |
|------|------|
| Framework | React 18 + TypeScript |
| Build | Vite 5 |
| State | Zustand 4 |
| Styling | Tailwind CSS 3 |
| Charts | Recharts |
| Icons | Lucide React |
| Realtime | WebSocket |

## 시작하기

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 빌드
npm run build
```

## 프로젝트 구조

```
src/
├── components/
│   ├── Toolbar.tsx        # 좌측 네비게이션
│   ├── Header.tsx         # 상단 바 (모델/모드 선택)
│   ├── QueryBox.tsx       # 연구 질문 입력
│   ├── ChatBox.tsx        # 대화 메시지
│   ├── StepCard.tsx       # 파이프라인 스텝 카드
│   └── Panels.tsx         # Left/Right 패널 + Modal
├── stores/
│   └── useStore.ts        # Zustand 4-Slice Store
├── hooks/
│   └── useWebSocket.ts    # WebSocket 연결 관리
├── types/
│   └── index.ts           # TypeScript 타입 정의
├── utils/
│   └── demo.ts            # 데모 시뮬레이터
├── styles/
│   └── globals.css        # Tailwind + 커스텀 스타일
├── App.tsx                # 루트 컴포넌트
└── main.tsx               # 엔트리포인트
```

## 데모 모드

현재 백엔드 없이 **Demo Simulator**로 동작합니다.
`src/utils/demo.ts`에서 더미 데이터를 수정할 수 있습니다.

## 환경 변수

```env
VITE_WS_URL=ws://localhost:8000/ws
VITE_API_URL=http://localhost:8000/api
```

## 라이선스

AIGEN Sciences - Confidential

---

**AIGEN Sciences × Modulabs | Phase 1: Aigen-E1**
